---
name: 策略啟用/禁用切換功能
status: open
created: 2025-12-09T17:45:09Z
updated: 2025-12-09T18:45:32Z
github: https://github.com/kennyto266/cbsc-system-unification/issues/9
depends_on: [001, 002, 003, 004]
parallel: true
conflicts_with: [008]
---

# Task: 策略啟用/禁用切換功能

## Description
實現4種CBSC策略的啟用/禁用切換功能，為個人提供直觀的策略管理控制界面。

## Acceptance Criteria
- [ ] 實現策略開關按鈕，支持點擊切換啟用/禁用狀態
- [ ] 添加切換確認對話框，防止誤操作
- [ ] 實現狀態持久化，切換後狀態能夠保存到後端
- [ ] 添加切換狀態視覺反饋，包括加載狀態和成功/失敗提示
- [ ] 實現策略切換的批量操作，支持一鍵啟用/禁用所有策略

## Technical Details

### UI組件設計
```javascript
// 策略切換按鈕組件
const createStrategyToggle = (strategyName, isEnabled) => {
  return `
    <div class="strategy-toggle">
      <label class="toggle-label">${strategyName}</label>
      <button
        class="toggle-btn ${isEnabled ? 'enabled' : 'disabled'}"
        onclick="toggleStrategy('${strategyName}')"
        data-strategy="${strategyName}"
      >
        <span class="toggle-slider"></span>
        <span class="toggle-text">${isEnabled ? '已啟用' : '已禁用'}</span>
      </button>
    </div>
  `;
};
```

### API接口調用
```javascript
// 策略切換API
async function toggleStrategy(strategyName, enabled) {
  try {
    const response = await fetch(`/api/strategies/${strategyName}/toggle`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ enabled: enabled })
    });

    if (response.ok) {
      const result = await response.json();
      return { success: true, data: result };
    } else {
      throw new Error(`HTTP ${response.status}: ${response.statusText}`);
    }
  } catch (error) {
    return { success: false, error: error.message };
  }
}
```

### 狀態管理
```javascript
// 策略狀態管理
class StrategyStateManager {
  constructor() {
    this.strategies = {
      DirectRSIStrategy: false,
      SentimentMomentumStrategy: false,
      CompositeIndexStrategy: false,
      VolatilityAdjustedStrategy: false
    };
    this.listeners = [];
  }

  // 切換策略狀態
  async toggleStrategy(strategyName) {
    const currentState = this.strategies[strategyName];
    const newState = !currentState;

    // 顯示確認對話框
    const confirmed = await this.showConfirmDialog(
      `確定要${newState ? '啟用' : '禁用'}策略 "${strategyName}"嗎？`
    );

    if (!confirmed) return false;

    // 調用API
    const result = await toggleStrategy(strategyName, newState);

    if (result.success) {
      this.strategies[strategyName] = newState;
      this.notifyListeners(strategyName, newState);
      return true;
    } else {
      this.showError(`切換策略失敗: ${result.error}`);
      return false;
    }
  }

  // 批量切換
  async toggleAllStrategies(enabled) {
    const confirmed = await this.showConfirmDialog(
      `確定要${enabled ? '啟用' : '禁用'}所有策略嗎？`
    );

    if (!confirmed) return;

    const results = [];
    for (const strategyName in this.strategies) {
      if (this.strategies[strategyName] !== enabled) {
        const result = await this.toggleStrategy(strategyName, false); // 不需要確認對話框
        results.push({ strategyName, result });
      }
    }

    const successCount = results.filter(r => r.result).length;
    const totalCount = results.length;

    this.showNotification(
      `批量操作完成：${successCount}/${totalCount} 個策略切換成功`,
      successCount === totalCount ? 'success' : 'warning'
    );
  }
}
```

## Dependencies
- [ ] Task 001: 基礎頁面結構和樣式開發
- [ ] Task 002: API接口集成和數據獲取
- [ ] Task 003: 策略列表和數值顯示組件
- [ ] Task 004: Chart.js集成和基礎圖表

## Effort Estimate
- Size: M
- Hours: 16
- Parallel: true

## Definition of Done
- [ ] 策略開關按鈕實現完成，點擊響應正常
- [ ] 確認對話框功能正常，防止誤操作
- [ ] 狀態持久化功能完成，切換狀態保存成功
- [ ] 視覺反饋機制完善，用戶體驗良好
- [ ] 批量操作功能可用，提高操作效率
- [ ] 錯誤處理機制健全，異常情況優雅處理
- [ ] 代碼審查通過，註釋完整清晰
- [ ] 集成測試通過，與整體系統功能協調