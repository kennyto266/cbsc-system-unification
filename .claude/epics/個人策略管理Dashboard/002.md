---
name: API集成和数据获取
epic: 個人策略管理Dashboard
status: backlog
priority: high
assigned:
effort: 3 days
parallel: false
depends_on: [1]
conflicts_with: []
labels: [frontend, phase-1, api]
created: 2025-12-18T10:36:00Z
updated: 2025-12-18T10:36:00Z
---

# Task 2: API集成和数据获取

## Description
实现与FastAPI后端的API集成，包括策略数据获取、错误处理、数据缓存和实时更新机制。

## Acceptance Criteria
- [ ] 实现FastAPI客户端
- [ ] 获取策略列表数据
- [ ] 获取策略详细性能数据
- [ ] 实现错误处理和重试机制
- [ ] 建立本地数据缓存
- [ ] 实现WebSocket实时更新（可选）

## Technical Details

### API客户端实现
```javascript
class APIClient {
    constructor(baseURL) {
        this.baseURL = baseURL;
        this.timeout = 5000;
    }

    async getStrategies() {
        // GET /api/strategies
    }

    async getStrategyPerformance(id, period) {
        // GET /api/strategies/{id}/performance?period=1m
    }

    async getStrategyDetail(id) {
        // GET /api/strategies/{id}
    }

    async startStrategy(id) {
        // POST /api/strategies/{id}/start
    }

    async stopStrategy(id) {
        // POST /api/strategies/{id}/stop
    }

    async deleteStrategy(id) {
        // POST /api/strategies/{id}/delete
    }
}
```

### 数据模型
```javascript
// 策略基础信息
const Strategy = {
    id: string,
    name: string,
    description: string,
    status: "running" | "stopped" | "error",
    current_return: number,
    sharpe_ratio: number,
    max_drawdown: number,
    created_at: string,
    updated_at: string
};

// 性能数据
const PerformanceData = {
    strategy_id: string,
    period: string,
    daily_returns: number[],
    cumulative_returns: number[],
    drawdowns: number[],
    sharpe_ratios: number[],
    timestamps: string[]
};

// 统计信息
const Statistics = {
    total_return: number,
    annualized_return: number,
    sharpe_ratio: number,
    max_drawdown: number,
    win_rate: number,
    profit_factor: number,
    total_trades: number,
    average_trade_duration: number
};
```

### 错误处理策略
- 网络超时处理
- API错误响应处理
- 数据格式验证
- 优雅降级机制
- 用户友好的错误提示

### 缓存机制
- LocalStorage数据缓存
- 缓存过期策略（5分钟）
- 离线模式支持
- 缓存更新通知

### WebSocket集成（可选）
```javascript
class WebSocketClient {
    constructor(url) {
        this.url = url;
        this.reconnectAttempts = 0;
        this.maxReconnectAttempts = 5;
    }

    connect() {
        // WebSocket连接逻辑
    }

    subscribe(strategyUpdates) {
        // 订阅策略更新
    }

    onMessage(callback) {
        // 消息处理
    }
}
```

## Dependencies
- FastAPI后端服务必须运行
- 后端API文档可用
- 网络连接稳定
- 浏览器支持Fetch API

## Deliverables
- [ ] `js/api.js` - API客户端实现
- [ ] `js/storage.js` - 本地存储管理
- [ ] `js/websocket.js` - WebSocket客户端（可选）
- [ ] `js/error-handler.js` - 错误处理模块
- [ ] API集成测试文件
- [ ] API接口文档

## Definition of Done
- [x] API客户端成功连接FastAPI
- [x] 能够获取策略列表数据
- [x] 能够获取策略性能数据
- [x] 错误处理机制正常工作
- [x] 数据缓存功能正常
- [x] 离线模式基本可用
- [ ] WebSocket实时更新（如实现）